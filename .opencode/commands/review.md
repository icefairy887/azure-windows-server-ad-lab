---
description: Review the AD lab without deploying it
---

Read `AGENTS.md`, `SECURITY.md`, `integrations/repository-context.md`, and `integrations/prompts/review-repository.md`.

Perform the repository review described by that prompt. Run only offline checks. Do not connect to Azure and do not execute scripts under `powershell/azure` or `powershell/active-directory`.

